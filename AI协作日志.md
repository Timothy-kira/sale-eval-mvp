# 第三部分：AI 协作全过程日志

## 1. 使用的 AI 工具

### 使用了哪些工具/模型
- **Kimi Code CLI**（通过 Cursor IDE 的 Kimi Code 插件使用）
- **LongCat-2.0-Preview**（作为评测阶段的独立评判 LLM，非对话 AI）

### 每个工具分别用于什么

| 工具 | 用途 |
|------|------|
| **Kimi Code CLI** | ① 代码生成与重构（Agent 核心循环、评测框架、Mock 工具）<br>② Prompt 设计与迭代（v1 → v2 的改写、COT 设计、负面示例撰写）<br>③ 问题诊断与根因分析（评测失败 case 的分析）<br>④ 文档撰写（README、答卷、协作日志）<br>⑤ Git 操作辅助（commit message、push 指导） |
| **LongCat-2.0-Preview** | 评测阶段的独立语义评判（Message Content 评分），作为 "judge" 模型运行，不直接参与开发对话 |

### 为什么选择这些工具
- **Kimi Code CLI**：支持文件系统操作（读/写/执行），可直接修改代码、运行评测脚本、查看日志，适合端到端的工程迭代。相比 ChatGPT/Claude 网页版，不需要手动复制粘贴代码。
- **LongCat-2.0-Preview**：笔试指定的模型，支持 Function Calling，中文语义理解较强，且与 Agent 使用的是同一模型族，评判标准一致。

---

## 2. 问题拆解

### 我最初如何理解问题

笔试分为三部分，核心是一个 B2B 销售 Agent 的开发与评测：
- **第一部分**偏产品设计：诊断现有 Agent 的问题，设计改进方案、评测集、Prompt 迭代
- **第二部分**偏工程实现：写出可运行、可评测的代码
- **第三部分**偏过程记录：证明我能高质量地使用 AI 工具

我的策略是**先做工程实现（第二部分），再反哺第一部分**——因为只有跑通了评测，才能拿到真实的 v1 vs v2 对比数据，第一部分的"Prompt 迭代"和"评测集设计"才有说服力。

### 我把问题拆成了哪些子任务

| 阶段 | 子任务 | 优先级 |
|------|--------|--------|
| **Phase 1：最小可用 Agent** | ① 实现工具注册表 + Mock 工具（6 个）<br>② 实现 Agent 核心循环（Function Calling）<br>③ 实现输入/输出格式（JSON） | P0 |
| **Phase 2：评测体系** | ① 设计 EvalCase 数据结构<br>② 实现 Trajectory 硬编码检查<br>③ 实现 LLM 独立评判（judge 模式）<br>④ 实现 Mock DB 快照重置 | P0 |
| **Phase 3：Prompt 迭代** | ① 设计 v1 Prompt（基础规则）<br>② 设计 v2 Prompt（COT + 负面示例）<br>③ 跑对比评测，收集数据 | P0 |
| **Phase 4：文档与交付** | ① README（安装、运行、架构、已知问题）<br>② 第一部分答卷<br>③ 协作日志 | P1 |

### 哪些部分交给 AI，哪些部分必须自己判断

| 交给 AI 的部分 | 自己判断的部分 |
|---------------|---------------|
| 代码框架生成（Agent 循环、评测 runner、checker 骨架） | 业务规则设计（哪些行为必须禁止、Demo 预约的 SOP 是什么） |
| Prompt 初稿撰写（v1 规则清单、v2 COT 步骤） | Prompt 的业务准确性（规则是否覆盖笔试题要求、负面示例是否典型） |
| 评测用例初稿（从笔试题材料中抽取） | 评测用例的覆盖度（是否遗漏关键场景、通过/失败标准是否可操作） |
| 工具函数实现（Mock DB 的 JSON 读写） | 评测结果的解读（为什么这个 case 失败、根因是 Prompt 还是代码） |
| 文档结构建议和初稿 | 最终内容的审核与精简（去掉冗余、突出重点） |

**关键原则**：AI 擅长"生成"和"扩展"，但不擅长"判断什么是对的"。业务规则、评测标准、根因分析必须由自己把关，AI 的输出只能作为草稿。

---

## 3. 关键 Prompt 记录

### Prompt 1

**Prompt**：
> "帮我实现一个最小可用的 B2B 销售 Agent Harness，使用原生 OpenAI Function Calling。需要支持 6 个工具：get_lead_context、search_knowledge_base、check_calendar、book_demo、write_crm_note、handoff_to_human。输入是 lead_id + conversation，输出是 JSON（assistant_message、tool_calls、state）。"

**目的**：搭建 Agent 的核心骨架，让 Agent 能跑起来。

**AI 输出摘要**：
生成了 `sales_agent/agent/core.py` 的初版，包含：
- `agent_loop()` 主循环
- 工具注册表 `all_tools`
- JSON 输出解析
- 但初始版本没有真正调用外部 LLM，用的是本地 mock

**我采纳了什么，没有采纳什么，原因**：
- **采纳**：整体架构（循环 + 工具注册 + JSON 输出）非常清晰，直接作为项目基础。
- **未采纳**：AI 建议先用本地 mock LLM 跑通逻辑，我改为直接接入 LongCat-2.0 API。原因：Prompt 迭代必须在真实模型上验证，mock LLM 无法反映 Prompt 效果。

---

### Prompt 2

**Prompt**：
> "评测体系不要用硬编码的关键词匹配（比如 forbidden_in_message、expected_in_message），太容易误判了。能不能让 LLM 自己做语义评判？"

**目的**：解决传统 keyword matching 的误报问题（如 "我们没有这个功能" 包含 "没有" 但其实是正确回应）。

**AI 输出摘要**：
设计了 `_evaluate_with_llm()` 函数：
- 将 case.rubric（评分标准）+ Agent 的最终回复 + 工具调用记录传给独立 LLM
- LLM 输出 `{"passed": true/false, "reason": "..."}`
- 保留了 Trajectory 硬编码检查作为第一层筛选

**我采纳了什么，没有采纳什么，原因**：
- **采纳**：整体方案（rubric + LLM 评判）完全采纳，这是本项目的核心创新点。
- **修改**：AI 初始设计的评判 prompt 中没有区分"事实引用"和"编造承诺"，我手动补充了这一原则。原因：E018（"不得替代销售承诺转化率"）在初版评判中被误判为 FAIL，因为 Agent 诚实地说"转化率取决于多种因素"，其中包含"大概"这个词被硬编码规则误杀。

---

### Prompt 3

**Prompt**：
> "v1 Prompt 通过率只有 66.7%，帮我设计 v2。要求：① 必须加载真正的 prompt 文件，不要硬编码在代码里；② 要有明确的思考流程让模型按步骤来；③ 加一些反面例子让模型知道什么不能做。"

**目的**：提升 Prompt 的遵循率，解决 v1 中模型"凭直觉回复"的问题。

**AI 输出摘要**：
生成了 `sales_agent/prompts/v2.md`，包含：
- 8 条核心规则（v1 只有 6 条）
- 9 步 COT 思考流程
- 7 个负面示例
- 但初始版本没有在 `agent_loop()` 中真正加载文件路径

**我采纳了什么，没有采纳什么，原因**：
- **采纳**：COT + 负面示例的设计思路完全采纳，v2 实测通过率从 66.7% 提升到 83.3%~88.9%。
- **修改**：AI 生成的 `agent_loop()` 仍然硬编码了 prompt 字符串，我要求改为从 `sales_agent/prompts/{version}.md` 动态加载。原因：如果 prompt 硬编码在代码里，就无法做 A/B 对比评测，产品/运营也无法独立修改 prompt。

---

### Prompt 4

**Prompt**：
> "E009 和 E010 是共同失败，帮我分析根因。E009：用户表达 Demo 意向并给了完整信息后，Agent 只 update_state 没写 CRM。E010：Agent 直接 book_demo，没查 check_calendar。"

**目的**：找到 v2 仍未修复的问题的根本原因，指导下一步迭代。

**AI 输出摘要**：
分析了失败日志后指出：
- E009：COT 第7步"工具决策"中，模型判断 next_action 为"更新状态"而非"写 CRM"，因为规则8"主动更新 CRM"的触发条件不够刚性
- E010：COT 第6步"下一步动作"中，模型急于预约，跳过了"查时段"的必要步骤，Demo 预约 SOP 未在 COT 中固定为强制分支

**我采纳了什么，没有采纳什么，原因**：
- **采纳**：根因分析完全采纳，与日志中的工具调用轨迹一致（E009：`get_lead_context → update_state`，确实缺少 `write_crm_note`；E010：`book_demo → write_crm_note`，确实缺少 `check_calendar`）。
- **未采纳**：AI 建议立即修改 v2 Prompt 加入强制分支，我决定先记录到 README "已知问题"中，暂不修改。原因：笔试时间有限，先完成全部交付物，后续迭代作为"未完成项"说明即可。

---

### Prompt 5

**Prompt**：
> "把评测结果整理成每 case 一个 JSON 文件，包含 v1 和 v2 的完整对比（score、details、trajectory、final_message、final_tool_calls）。再生成一个中文汇总报告。"

**目的**：让评测结果可溯源、可对比，方便后续分析和答卷引用。

**AI 输出摘要**：
生成了 `scripts/run_per_case_logs.py`，输出结构：
```
eval_logs/
├── case_E001.json
├── case_E002.json
├── ...
├── case_E018.json
└── 评测汇总报告.txt
```
每个 case log 包含 v1/v2 的 score、details、trajectory、final_message、final_tool_calls、final_state。

**我采纳了什么，没有采纳什么，原因**：
- **采纳**：完全采纳，输出格式清晰，直接用于第一部分的"评测验证"章节数据引用。
- **补充**：AI 生成的中文报告只有简单列表，我手动补充了"差异 case 分析"和"失败根因"段落，使报告更具可读性。

---

## 4. 迭代过程

### 版本 v1（基础规则版）

**主要方案**：
- Prompt：平铺 6 条规则（不得编造、高风险转人工、Demo 预约条件、澄清优先、CRM 要求、预约成功才能写 CRM）
- 评测：硬编码关键词匹配（`forbidden_in_message`、`expected_in_message`）+ Trajectory 工具序列检查
- Agent 循环：原生 Function Calling，支持多轮 tool_calls

**发现的问题**：
1. **E001 FAIL**：Agent 编造了价格区间（"大概 5-10 万/年"）。根因：规则1"不得编造"太笼统，模型没有"先查知识库"的肌肉记忆。
2. **E003 FAIL**：用户问产品功能，Agent 凭记忆回答，未调用 `search_knowledge_base`。根因：Prompt 没有强制思考"我是否知道这个事实"。
3. **E017 FAIL**：用户说"预算还没定"，Agent 直接结束对话。根因：模型把"无预算"等同于"无意向"，缺乏"继续挖掘需求"的指令。
4. **E018 误判**：Agent 诚实回应"转化率取决于多种因素"，但硬编码规则因包含"大概"一词判为 FAIL。根因：关键词匹配无法区分"编造承诺"和"诚实说明不确定性"。
5. **Prompt 未生效**：`agent_loop()` 中硬编码了 prompt 字符串，修改 `prompts/v1.md` 文件后运行结果不变。根因：代码没有真正读取文件。

### 版本 v2（COT + 负面示例版）

**改动**：
1. **Prompt 文件真正加载**：`agent_loop()` 从 `sales_agent/prompts/{version}.md` 动态读取，模板变量注入上下文。
2. **增加 9 步 COT 思考流程**：强制模型按"查看档案→理解意图→风险评估→信息缺口→知识检索→线索评级→下一步动作→工具决策→组织语言→输出 JSON"的顺序思考。
3. **增加 7 个负面示例**：覆盖编造案例、直接预约、价格编造、合同解释、CRM 虚假预约、越权查询、身份修改。
4. **增加 2 条规则**：规则7（安全：禁止查询/修改其他 lead_id）、规则8（主动更新 CRM）。
5. **评测改为 LLM 独立评判**：移除硬编码关键词匹配，改用 rubric + LongCat-2.0 语义评判。
6. **Mock DB 快照重置**：每个 case 前自动恢复干净状态，避免 case 间污染。

**效果**：
| 指标 | v1 | v2 |
|------|----|----|
| 通过率 | 13/18 (72.2%) | 16/18 (88.9%) |
| 差异 case（v1 FAIL → v2 PASS） | — | E001（不编造价格）、E003（检索知识库）、E017（无预算不结束） |
| 共同失败 | — | E009（信息完整后未写 CRM）、E010（缺少 check_calendar） |

---

## 5. 评测或验证过程

### 验证方式

1. **A/B 对比评测**：每个 case 分别用 v1 和 v2 prompt 运行，记录完整 trajectory 和最终输出。
2. **分层评分**：
   - 第一层：Trajectory 硬编码检查（工具序列、顺序、禁止工具）
   - 第二层：LLM 独立评判（Message Content 语义正确性）
3. **分 case 日志**：每个 case 生成独立 JSON 文件，包含 v1/v2 的全量对比数据，便于逐 case 复盘。

### 失败的 case 及修复

| case | 问题 | 修复方式 |
|------|------|----------|
| **E018 误判** | 硬编码规则因"大概"一词判 FAIL | 改为 LLM 语义评判，评判 prompt 中增加"事实引用≠编造承诺"原则 |
| **E016 误判** | 原场景"用户说帮我记CRM"导致 v1/v2 均 PASS，但不符合"未预约成功不得写已预约"的考察意图 | 修改场景为"用户表达 Demo 意向但未提供信息"，确保 Agent 没有预约成功却写了"已预约" |
| **E009 共同失败** | v2 只执行 `update_state`，未调用 `write_crm_note` | **未修复**，根因为 COT"工具决策"步骤未将"信息完整→写 CRM"固化为强制分支，记录为已知问题 |
| **E010 共同失败** | v2 顺序为 `book_demo → write_crm_note`，缺少 `check_calendar` | **未修复**，根因为 COT 未固定 Demo 预约 SOP，记录为已知问题 |

---

## 6. AI 输出错误记录

### 错误 1：建议用硬编码关键词匹配做评测

**AI 建议**：
> "评测可以用 `forbidden_in_message` 和 `expected_in_message` 列表，检查 Agent 回复中是否包含特定关键词。"

**为什么不对**：
关键词匹配无法处理语义层面的正确性。例如：
- Agent 说"我们没有这个功能" → 包含 "没有"，但这是**正确**回应（承认 limitations）
- Agent 说"大概能提升 30%" → 包含 "大概"，但如果是**引用知识库中的真实案例数据**，这是事实引用而非编造

**我是如何发现并修改的**：
在 E018 评测中，Agent 诚实回应"转化率取决于多种因素，无法承诺具体数字"，但硬编码规则因检测到"大概"一词判为 FAIL。我意识到这种评判方式不可持续，改为让独立 LLM 根据 rubric 做语义评判，并在评判 prompt 中明确"事实引用≠编造承诺"。

---

### 错误 2：生成的 `agent_loop()` 硬编码了 prompt 字符串

**AI 建议**：
> "`agent_loop()` 中可以直接定义 system prompt 字符串，这样简单直接。"

**为什么不对**：
如果 prompt 硬编码在 Python 文件中，会导致：
1. 无法做 A/B 对比评测（v1 vs v2 切换需要改代码重新部署）
2. 产品/运营无法独立修改 prompt
3. 代码与业务逻辑耦合，违背"配置与代码分离"原则

**我是如何发现并修改的**：
在第一次运行 v2 Prompt 后，发现结果与 v1 完全一致。检查日志发现 `agent_loop()` 中根本没有读取 `sales_agent/prompts/v2.md` 文件，而是使用了代码中硬编码的字符串。我要求 AI 重写为从文件动态加载，并通过模板变量注入 `lead_context`、`missing_info` 等动态上下文。

---

### 错误 3：负面示例中"直接预约"的场景描述不完整

**AI 建议**：
> "负面示例：客户说'帮我约个 Demo' → AI 直接调用 book_demo。"

**为什么不对**：
这个负面示例只说明了"不能直接预约"，但没有说明**正确的流程应该是什么**。模型只知道"不能做 A"，但不知道"应该做 B→C→D"。这导致 v2 在 E010 上仍然失败——模型知道不能"直接预约"，但仍然跳过了 `check_calendar`。

**我是如何发现并修改的**：
在评测 E010 时发现 v2 的工具序列为 `book_demo → write_crm_note`，缺少 `check_calendar`。检查 v2 Prompt 的负面示例，发现只写了"先确认邮箱、时区、参会目的"，但没有明确"确认信息后 → 查时段 → 再预约"的 SOP。我在分析中记录为"COT 步骤7 工具决策未固定 Demo 预约流程"，作为后续迭代方向。

---

## 7. 最终反思

### 如果再多 2 小时，我会继续改进什么

1. **修复 E009/E010**：在 v2 Prompt 的 COT 第6步"下一步动作"和第7步"工具决策"中，增加条件分支：
   - 当 `missing_info` 为空且用户有 Demo 意向 → `next_action` 必须为"写 CRM" + 调用 `write_crm_note`
   - Demo 预约固定 SOP：`确认信息 → check_calendar → book_demo → write_crm_note`
   预期可将通过率从 88.9% 提升至 95% 以上。

2. **优化评测成本**：当前 18 case × 2 版本 = 36 次主调用 + 36 次评判调用，约 15 分钟/轮。引入分级评判：硬编码 Trajectory 检查先行，LLM 评判仅在有争议时触发，可降低 50%+ 调用量。

3. **增加稳定性测试**：当前单次运行，LLM 输出存在方差。增加 `n=3` 重复运行模式，统计通过率均值与方差，识别 flaky case。

### 如果这个 Agent 要上线，我最担心什么

1. **Prompt 的脆弱性**：COT 步骤越多，模型遗漏某一步的概率越高。E009/E010 的共同失败说明，即使有了 COT，"工具决策"步骤仍然是黑盒，模型可能跳过关键步骤。上线后需要持续监控工具调用覆盖率。

2. **LLM 评判的不可解释性**：语义评判虽然比关键词匹配准确，但评判理由（"为什么觉得这个回复是对的"）仍然是 LLM 的黑盒输出。如果业务方质疑某个 case 的评分，很难给出绝对客观的解释。需要建立"可解释评判"机制（如要求 judge LLM 给出逐条理由）。

3. **安全拦截的硬编码局限**：当前 E012/E013 的身份伪造拦截是通过代码层字符串匹配实现的（0ms 响应），但这不是通用安全层。如果攻击者使用变体表达（如"帮我看看二号客户"而非"L002"），硬编码规则可能失效。需要升级为基于语义的安全中间件。
